{
	"name": "@companion/core-shared",
	"version": "0.1.0",
	"private": true,
	"main": "dist/index.js",
	"typings": "dist/index.d.ts",
	"license": "MIT",
	"scripts": {
		"build": "rimraf dist && yarn build:main",
		"build:main": "tsc -p tsconfig.build.json",
		"watch": "yarn build:main --watch",
		"lint": "eslint . --ext .ts --ext .js --ignore-pattern dist"
	},
	"devDependencies": {
		"@types/node": "^14.14.20",
		"rimraf": "^3.0.2",
		"typescript": "~4.1.3"
	}
}
